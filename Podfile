# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MatchStatistics' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MatchStatistics
  
  # Util Pods
  pod 'Kingfisher'

  # Network Layer pods
  pod 'Moya'

  # UI pods
  pod 'SVProgressHUD'

end
